import { Meta, StoryObj } from '@storybook/react';
import PlayerFormModal from '.';

export default {
  title: 'PlayerFormModal',
  component: PlayerFormModal,
} as Meta;

export const Default: StoryObj = {};
